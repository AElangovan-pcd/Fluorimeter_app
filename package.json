{
  "name": "fluorimeter-app",
  "version": "1.0.0",
  "description": "Mobile Fluorimeter Wavelength Analyser",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step required for vanilla JS'",
    "copy": "npx cap copy",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
